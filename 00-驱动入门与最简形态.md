# 第 0 课：驱动入门与最简形态

目标：搞清 **驱动在系统里干什么**，以及 **最小的“驱动形态”长什么样**。

---

## 1. 一句话定义

**设备驱动 = 内核里的一段代码，负责把“硬件细节”翻译成“软件接口”。**

用户程序不应该知道：LED 接在哪个 GPIO、寄存器地址是多少、要写哪一位。  
用户程序只说：`把灯打开`。驱动负责：找到管脚、拉高电平。

用 RV1126B 板子举例：

| 层 | 谁 | 干什么 |
|---|---|---|
| 用户 | `echo 1 > /sys/class/leds/.../brightness` | 想开灯 |
| 驱动 | `leds-gpio.c` | 把“开灯”变成写 GPIO |
| 硬件 | 某颗 `gpioX` 管脚 | 真正点亮 LED |

---

## 2. 用户态 vs 内核态

```
┌─────────────────────────────┐
│  用户态 (User Space)         │  ← App、shell、你平时写的 C 程序
│  不能直接碰寄存器            │
└──────────────┬──────────────┘
               │ 系统调用 open/read/write/ioctl
               │ 或访问 /sys、/dev
┌──────────────▼──────────────┐
│  内核态 (Kernel Space)       │  ← 设备驱动跑在这里
│  可以读寄存器、处理中断      │
└──────────────┬──────────────┘
               │
         真实硬件 (GPIO / I2C / VPU …)
```

三条铁律：

1. **驱动跑在内核**，崩溃可能整机重启。
2. **用户程序不能直接写物理寄存器**，必须经驱动。
3. 驱动暴露接口的常见方式：`/dev/xxx`、`/sys/class/...`、网络协议栈等。

---

## 3. 最简形态：内核模块（还不是“设备驱动”）

设备驱动通常做成 **可加载内核模块（.ko）**。比硬件驱动更简单的是：只打印日志的模块。

```c
#include <linux/module.h>
#include <linux/init.h>

static int __init hello_init(void)
{
	pr_info("hello: loaded\n");
	return 0;
}

static void __exit hello_exit(void)
{
	pr_info("hello: unloaded\n");
}

module_init(hello_init);
module_exit(hello_exit);
MODULE_LICENSE("GPL");
```

| 动作 | 命令（板上） | 发生什么 |
|---|---|---|
| 装入 | `insmod hello.ko` | 调 `hello_init` |
| 查看 | `dmesg \| tail` | 看到 `hello: loaded` |
| 卸载 | `rmmod hello` | 调 `hello_exit` |

这还不是设备驱动——它**没有绑定任何硬件**。  
真正的设备驱动 = **模块 + 匹配某个设备 + 在匹配成功时初始化硬件**。

---

## 4. 本板最简单的真驱动：GPIO LED

板级设备树中常见工作灯（飞凌示例 `FET1126B-S.dtsi`）：

```dts
leds: leds {
	compatible = "gpio-leds";
	pinctrl-names = "default";
	pinctrl-0 = <&work_led_gpio>;
	work_led: work {
		gpios = <&gpio0 RK_PA3 GPIO_ACTIVE_LOW>;
		linux,default-trigger = "heartbeat";
	};
};
```

正点原子板类似，见 `rv1126b-alientek.dtsi`（管脚可能是 `RK_PC5` 等）。

对应驱动：

`kernel-6.1/drivers/leds/leds-gpio.c`

### 4.1 启动后链路

```
设备树: compatible = "gpio-leds"
        gpios = <&gpio0 ...>
              │
              ▼ 内核解析 DT，创建 platform_device
              │
              ▼ 驱动表匹配 "gpio-leds"
        leds-gpio.c 的 probe 被调用
              │
              ▼ 申请 GPIO，注册到 LED 子系统
              │
              ▼ 用户可见:
        /sys/class/leds/work/brightness
```

### 4.2 真正开灯的函数

```c
static void gpio_led_set(struct led_classdev *led_cdev,
	enum led_brightness value)
{
	struct gpio_led_data *led_dat = cdev_to_gpio_led_data(led_cdev);
	int level = (value == LED_OFF) ? 0 : 1;

	gpiod_set_value(led_dat->gpiod, level);
}
```

用户写 `brightness=1` → LED 核心回调到这里 → `gpiod_set_value` 拉高管脚。

### 4.3 文件末尾注册

```c
module_platform_driver(gpio_led_driver);
```

这行宏：注册驱动、匹配设备、自动调 probe。第 1 课会拆开讲。

---

## 5. 和 MPP 对比（建立直觉）

| | GPIO LED（简单） | MPP（多媒体） |
|---|---|---|
| 硬件 | 1 根 GPIO | 整块编解码 IP |
| 接口 | `/sys/class/leds/...` | `/dev/mpp_service` |
| 代码量 | 约 300 行 | 一整棵 `mpp/` 目录 |
| 核心套路 | DT 匹配 → probe → 暴露接口 | 相同，接口更复杂 |

**套路相同，复杂度不同。** 从 LED 学起，再看 `mpp_service.c` 会顺很多。

---

## 6. 本课必记 5 个词

1. **用户态 / 内核态** — 驱动在内核  
2. **模块（.ko）** — 驱动的常见打包形式  
3. **设备树（DT）** — 描述“板上有什么硬件”  
4. **compatible** — 设备和驱动的匹配钥匙  
5. **probe** — 匹配成功后的初始化入口  

---

## 7. 课后练习

板上：

```bash
ls /sys/class/leds/

echo none > /sys/class/leds/work/trigger
echo 1 > /sys/class/leds/work/brightness
echo 0 > /sys/class/leds/work/brightness

cat /sys/class/leds/work/device/of_node/compatible
# 期望: gpio-leds
```

思考：你写的 `1` 怎样到达 `gpio_led_set()`，再到具体 GPIO 管脚。

---

## 下一课

[第 1 课：Platform 驱动骨架](./01-Platform驱动骨架.md)
