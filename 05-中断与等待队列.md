# 第 5 课：中断与等待队列

目标：理解硬件“做完了”如何通知驱动，以及应用如何阻塞等待结果。

---

## 1. 为什么需要中断

外设完成一次操作（UART 收完字节、VPU 解完一帧）后，拉高中断线。  
驱动不应在 probe 里死等，而是：

```
启动硬件 → 当前线程可睡眠等待
        → 中断到来 → 唤醒等待者 → 继续处理
```

DT：

```dts
interrupts = <GIC_SPI 56 IRQ_TYPE_LEVEL_HIGH>;
```

驱动：

```c
irq = platform_get_irq(pdev, 0);
ret = devm_request_irq(dev, irq, my_isr, IRQF_SHARED, "mydev", priv);
```

---

## 2. 中断处理函数约束

```c
static irqreturn_t my_isr(int irq, void *dev_id)
{
	struct my_priv *priv = dev_id;

	/* 快速：读状态寄存器、清中断标志 */
	/* 不要：长时间循环、可能睡眠的锁、大量内存分配 */

	wake_up_interruptible(&priv->wq);
	return IRQ_HANDLED;   /* 或 IRQ_NONE / IRQ_WAKE_THREAD */
}
```

| 返回值 | 含义 |
|---|---|
| `IRQ_HANDLED` | 是我的中断，已处理 |
| `IRQ_NONE` | 不是我的（共享 IRQ） |
| `IRQ_WAKE_THREAD` | 硬中断里只做最少事，唤醒线程中断 |

**上半部 / 下半部**：硬 ISR 要短；耗时工作放到 threaded irq、`tasklet`、`workqueue`、kthread。

MPP 里常见：中断里记录状态 + 唤醒任务队列里的 worker（`kthread_worker`）。

---

## 3. 等待队列：让 read/ioctl 睡得合法

```c
#include <linux/wait.h>

wait_queue_head_t wq;
init_waitqueue_head(&wq);

/* 用户调用 read/ioctl 时 */
wait_event_interruptible(wq, priv->done);

/* 中断或完成回调里 */
priv->done = 1;
wake_up_interruptible(&wq);
```

`wait_event_interruptible`：条件为假则睡眠；可被信号打断（返回 `-ERESTARTSYS` 等）。

与 completion 对比：

```c
struct completion cmp;
init_completion(&cmp);
wait_for_completion_interruptible(&cmp);
complete(&cmp);   /* 或 complete_all */
```

一次性“等完成”常用 completion；要反复等状态可用 wait queue。

---

## 4. 典型字符设备阻塞读

```c
static ssize_t my_read(struct file *f, char __user *buf, size_t len, loff_t *p)
{
	struct my_priv *priv = f->private_data;
	int ret;

	ret = wait_event_interruptible(priv->wq, priv->data_ready);
	if (ret)
		return ret;

	if (copy_to_user(buf, &priv->data, sizeof(priv->data)))
		return -EFAULT;

	priv->data_ready = 0;
	return sizeof(priv->data);
}
```

非阻塞：`open` 带 `O_NONBLOCK` 时，条件不满足应返回 `-EAGAIN`。

---

## 5. 在本 SDK 中的落点

| 驱动 | 中断用途 |
|---|---|
| `i2c-rk3x.c` | I2C 传输完成 |
| `8250_dw.c` | UART 收发 |
| `gpio-rockchip.c` | GPIO 边沿 → 子中断域 |
| `mpp/*.c` | 编解码完成，唤醒 session/任务队列 |

读 MPP 时关注：`platform_get_irq`、`devm_request_irq`、任务完成路径上的 `wake_up` / `complete`。

---

## 6. 调试提示

```bash
cat /proc/interrupts | head
# 看对应驱动名的计数是否在增加

dmesg | grep -i irq
```

中断风暴、忘记清状态位 → 计数狂涨或系统卡顿。

---

## 7. 检查题

1. ISR 里能否调用 `mutex_lock`？→ **通常不能**（可能睡眠）。用自旋锁时注意关中断版本。  
2. `IRQ_WAKE_THREAD` 的作用？→ 把耗时工作放到可睡眠的线程 ISR。  
3. 用户 `read` 卡住多半在等什么？→ `wait_event` 条件未满足 / 中断未到。

---

## 8. 练习

1. 在纸上画：ioctl 启动硬件 → wait_event → ISR → wake_up → ioctl 返回。  
2. 打开 `i2c-rk3x.c`，搜索 `request_irq` / `wait_`，标出传输完成路径。  
3. 板上对某外设操作前后对比 `/proc/interrupts` 计数。

---

## 下一课

[第 6 课：I2C 与 SPI 客户端驱动](./06-I2C与SPI客户端驱动.md)
