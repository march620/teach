# 第 2 课：设备树与资源获取

目标：看懂 DT 常见属性，并知道驱动里用哪个 API 去拿。

---

## 1. 设备树分层

```
rv1126b.dtsi                 ← SoC：寄存器、中断、时钟（芯片固定）
    ↑
板级 .dtsi（如 FET1126B-S）  ← 核心板：LED、电源
    ↑
板级 .dts（如 OK1126B-practice-linux.dts）← 使能外设、加节点
```

| 层 | 改什么 | 不该乱改 |
|---|---|---|
| `rv1126b.dtsi` | 几乎不动 | `reg` / `interrupts` |
| board `.dtsi` | 管脚、默认外设 | — |
| 你的 `.dts` | `status`、新设备节点 | 不要重抄 SoC 地址 |

`&label { ... }` = 引用已有节点并覆盖/追加属性。

---

## 2. 属性 ↔ API 总表

| DT 属性 | 含义 | 驱动 API |
|---|---|---|
| `compatible` | 匹配驱动 | `of_match_table` |
| `reg` | 物理地址+长度 | `platform_get_resource` / `devm_platform_ioremap_resource` |
| `interrupts` | 中断 | `platform_get_irq` |
| `clocks` + `clock-names` | 时钟 | `devm_clk_get(dev, "name")` |
| `resets` + `reset-names` | 复位 | `devm_reset_control_get` |
| `gpios` / `xxx-gpios` | GPIO | `devm_gpiod_get` 等 |
| `pinctrl-0` | 管脚复用 | 框架自动处理居多 |
| `status` | okay/disabled | okay 才激活设备 |
| `rockchip,...` | 厂商扩展 | `of_property_read_u32` 等 |

口诀：**DT 描述“有什么”，probe 用 API“拿过来用”。**

---

## 3. UART0：reg + irq + clk

`rv1126b.dtsi`：

```dts
uart0: serial@20810000 {
	compatible = "rockchip,rv1126b-uart", "snps,dw-apb-uart";
	reg = <0x20810000 0x100>;
	interrupts = <GIC_SPI 56 IRQ_TYPE_LEVEL_HIGH>;
	clocks = <&cru SCLK_UART0>, <&cru PCLK_UART0>;
	clock-names = "baudclk", "apb_pclk";
	status = "disabled";
};
```

- `reg = <基址 长度>`
- `clock-names` 与 `clocks` **按序一一对应**

驱动 `8250_dw.c`：

```c
regs = platform_get_resource(pdev, IORESOURCE_MEM, 0);
irq  = platform_get_irq_optional(pdev, 0);
p->membase = devm_ioremap(dev, regs->start, resource_size(regs));
data->clk  = devm_clk_get_optional(dev, "baudclk");
data->pclk = devm_clk_get_optional(dev, "apb_pclk");
```

更短写法（I2C 常见）：

```c
i2c->regs = devm_platform_ioremap_resource(pdev, 0);
irq = platform_get_irq(pdev, 0);
i2c->clk = devm_clk_get(&pdev->dev, "i2c");
```

---

## 4. GPIO：最简单的板级资源

```dts
practice_led {
	label = "practice_led";
	gpios = <&gpio0 RK_PC0 GPIO_ACTIVE_HIGH>;
	default-state = "off";
};
```

| 部分 | 意思 |
|---|---|
| `&gpio0` | 哪个 GPIO 控制器 |
| `RK_PC0` | 组内编号 |
| `GPIO_ACTIVE_HIGH` | 有效电平 |

驱动不写死管脚，用 `devm_fwnode_get_gpiod_from_child` / `devm_gpiod_get`。  
换管脚只改 DT（重编 dtb），驱动可不动。

GPIO bank 本身也是 platform 设备（带 `reg`/`interrupts`），且：

```dts
gpio-controller;
#gpio-cells = <2>;   /* 引用时跟 pin + flags */
```

---

## 5. 复杂节点：rkvdec（预告 MPP）

```dts
rkvdec: rkvdec@22140100 {
	compatible = "rockchip,rkv-decoder-rv1126b", ...;
	reg = <0x22140100 0x600>, <0x22140000 0x100>;
	reg-names = "regs", "link";
	interrupts = <GIC_SPI 137 IRQ_TYPE_LEVEL_HIGH>;
	clocks = <&cru ACLK_RKVDEC>, ...;
	clock-names = "aclk_vcodec", ...;
	resets = <&cru SRST_...>, ...;
	reset-names = "video_a", ...;
	rockchip,taskqueue-node = <1>;
	power-domains = <&power RV1126B_PD_VDO>;
	status = "disabled";
};
```

| 属性 | 作用 |
|---|---|
| `reg-names` | 多段 reg 按名取 |
| `resets` | 硬件复位块 |
| `power-domains` | 电源域 |
| `rockchip,*` | 私有属性，`of_property_read_*` |

---

## 6. status 与覆盖

SoC 默认常 `status = "disabled"`。板级打开：

```dts
&uart0 {
	status = "okay";
};
```

---

## 7. probe 推荐顺序

```
1. priv = devm_kzalloc
2. ioremap 寄存器
3. platform_get_irq + request_irq
4. clk_get + clk_prepare_enable
5. reset_control（必要时复位）
6. gpio / regulator
7. of_property_read_* 自定义属性
8. 注册字符设备 / 子系统
9. platform_set_drvdata
```

---

## 8. 检查题

1. `reg = <0x20810000 0x100>`：基址、长度。  
2. clocks 与 clock-names 数量应对齐。  
3. 改 DT 管脚一般不用改 `leds-gpio.c`。  
4. `disabled` 节点通常不会 probe。

---

## 9. 课后练习

**A.** 打开 `rv1126b.dtsi` 的 `i2c0`，写出基址、中断、时钟名；对照 `i2c-rk3x.c`。

**B.** 板上：

```bash
ls /sys/class/leds/
echo 1 > /sys/class/leds/practice_led/brightness
```

**C.**

```bash
find /proc/device-tree -name 'serial@20810000'
hexdump -C /proc/device-tree/serial@20810000/reg
```

---

## 下一课

[第 3 课：GPIO 与 LED 实战](./03-GPIO与LED实战.md)
