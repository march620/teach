# 第 4 课：字符设备

目标：理解 `/dev/xxx` 如何出现，以及 `open/read/write/ioctl` 如何进到驱动。

---

## 1. 什么是字符设备

字符设备：以**字节流**访问的设备，对应用户态的 `/dev/某节点`。

```
用户: fd = open("/dev/mpp_service", O_RDWR);
      ioctl(fd, CMD, arg);
        ↓
内核: file_operations.unlocked_ioctl
```

块设备（磁盘）、网络设备走别的子系统；RV1126B 上大量外设（MPP、很多 misc）是字符设备。

---

## 2. 注册一套字符设备的步骤

经典五步（MPP service 就是这样）：

```c
/* 1. 分配设备号 */
alloc_chrdev_region(&dev_id, 0, 1, "mpp_service");

/* 2. 初始化 cdev，绑定 fops */
cdev_init(&cdev, &rockchip_mpp_fops);
cdev.owner = THIS_MODULE;

/* 3. 把 cdev 加到内核 */
cdev_add(&cdev, dev_id, 1);

/* 4. 创建 class（若还没有） */
cls = class_create(THIS_MODULE, "mpp_class");

/* 5. 创建设备节点 → /dev/mpp_service */
device_create(cls, parent, dev_id, NULL, "mpp_service");
```

注销时大致反向：`device_destroy` → `cdev_del` → `unregister_chrdev_region` → `class_destroy`。

也可使用 `misc_register`（杂项设备，自动生成 `/dev`，代码更短），适合简单驱动。

---

## 3. file_operations：用户调用的落点

```c
static const struct file_operations rockchip_mpp_fops = {
	.owner		= THIS_MODULE,
	.open		= mpp_dev_open,
	.release	= mpp_dev_release,
	.poll		= mpp_dev_poll,
	.unlocked_ioctl	= mpp_dev_ioctl,
	.compat_ioctl	= mpp_dev_compat_ioctl,
	/* 有的驱动还有 .read / .write / .mmap */
};
```

| 用户态 | 内核回调 |
|---|---|
| `open` | `.open` |
| `close` | `.release` |
| `read` / `write` | `.read` / `.write` |
| `ioctl` | `.unlocked_ioctl` |
| `mmap` | `.mmap` |
| `select`/`poll` | `.poll` |

返回值：成功通常返回 0 或字节数；失败返回负的 errno（如 `-EINVAL`）。

---

## 4. ioctl：驱动最常见的“命令通道”

字符设备很少只用 read/write 传结构化命令，多用 ioctl：

```c
#define MY_CMD_SET _IOW('m', 1, int)
#define MY_CMD_GET _IOR('m', 2, int)

static long my_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)
{
	int val;

	switch (cmd) {
	case MY_CMD_SET:
		if (copy_from_user(&val, (void __user *)arg, sizeof(val)))
			return -EFAULT;
		/* 操作硬件 */
		return 0;
	case MY_CMD_GET:
		val = /* 读硬件 */;
		if (copy_to_user((void __user *)arg, &val, sizeof(val)))
			return -EFAULT;
		return 0;
	default:
		return -ENOTTY;
	}
}
```

注意：

- 用户指针必须用 `copy_from_user` / `copy_to_user`，不能直接解引用。
- MPP 的命令在 `mpp_show_support_cmd` / 头文件里可查（`MPP_CMD_*`）。

---

## 5. 对照 MPP：字符设备在 probe 里注册

`mpp_service.c` 中：

```c
static int mpp_register_service(struct mpp_service *srv, const char *service_name)
{
	alloc_chrdev_region(&srv->dev_id, 0, 1, service_name);
	cdev_init(&srv->mpp_cdev, &rockchip_mpp_fops);
	cdev_add(&srv->mpp_cdev, srv->dev_id, 1);
	srv->child_dev = device_create(srv->cls, dev, srv->dev_id,
				       NULL, "%s", service_name);
	return 0;
}
```

用户态最终看到：`/dev/mpp_service`。  
`external/mpp` 库打开该节点，用 ioctl 下发编解码任务。

结构：

```
platform 驱动 probe
    → 注册字符设备 /dev/mpp_service
    → 再注册 rkvdec/rkvenc 等子 platform 驱动
应用 → open → ioctl → 服务层分发到具体硬件驱动
```

---

## 6. 最小 misc 字符设备模板

```c
#include <linux/module.h>
#include <linux/miscdevice.h>
#include <linux/fs.h>
#include <linux/uaccess.h>

static ssize_t my_read(struct file *f, char __user *buf, size_t len, loff_t *p)
{
	const char *msg = "hello\n";
	size_t n = strlen(msg);

	if (*p >= n)
		return 0;
	if (copy_to_user(buf, msg + *p, n - *p))
		return -EFAULT;
	*p = n;
	return n - *p;
}

static const struct file_operations my_fops = {
	.owner = THIS_MODULE,
	.read  = my_read,
};

static struct miscdevice my_misc = {
	.minor = MISC_DYNAMIC_MINOR,
	.name  = "myhello",
	.fops  = &my_fops,
};

static int __init my_init(void)
{
	return misc_register(&my_misc);  /* → /dev/myhello */
}

static void __exit my_exit(void)
{
	misc_deregister(&my_misc);
}

module_init(my_init);
module_exit(my_exit);
MODULE_LICENSE("GPL");
```

板上：`cat /dev/myhello` → `hello`。

---

## 7. 检查题

1. `cdev_init` 和 `device_create` 缺一不可吗？→ 缺 `device_create`（且无 udev 规则）时可能没有 `/dev` 节点。  
2. 为何 ioctl 参数要用 `copy_from_user`？→ 用户指针不能在内核直接解引用。  
3. misc 和完整 cdev+class 怎么选？→ 简单单个节点用 misc；要复杂 class/多设备用完整方案（MPP）。

---

## 8. 练习

1. 写出纸上模板：platform + misc，`compatible = "myvendor,led-ctrl"`，ioctl 开关假想 LED。  
2. 板上：`ls -l /dev/mpp_service`，`cat /proc/devices | grep mpp`。  
3. 阅读 `mpp_service.c` 的 `mpp_register_service` / `mpp_remove_service`。

---

## 下一课

[第 5 课：中断与等待队列](./05-中断与等待队列.md)
